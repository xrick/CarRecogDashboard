# 工地看板 Site Board Dashboard — runtime dependencies
# (PyQt5 is pre-installed in the in-tree venv; pinned here for completeness)
PyQt5==5.15.11

# Backend
fastapi==0.136.1
uvicorn[standard]==0.47.0
pydantic==2.13.4

# Frontend networking (REST poll + SSE stream) — already in the venv
requests==2.34.0
